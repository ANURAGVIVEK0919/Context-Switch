# ContextSwitch Agent (Chunk A)

This package will contain agent logic for ContextSwitch. Dependencies: axios, js-yaml.

## Development

- Run `npm install` to install dependencies.
- Add agent logic in `src/index.ts`.
