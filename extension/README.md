# ContextSwitch VS Code Extension

This extension connects to the ContextSwitch backend WebSocket server and sends file change events.

## Development

- Run `npm install` to install dependencies.
- Use `npm run compile` to build.
- Press F5 in VS Code to launch the extension development host.
