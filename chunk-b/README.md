# ContextSwitch Backend (Chunk B)

Express + WebSocket backend for ContextSwitch.

## Development

- Run `npm install` to install dependencies.
- Use `npm run start` to start the backend (port 3001).
- Use `npm run ws` to start the WebSocket server (port 3002).
